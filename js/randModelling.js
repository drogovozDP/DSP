//for(let i = 1; i <= 1; i++) alert(3)



/*
let string = prompt("введите хуйню")
let subString = new Array()

let a = false
let b = false

if ((string.indexOf('a') != -1) || (string.indexOf('а')) != -1) a = true
if ((string.indexOf('b') != -1) || (string.indexOf('б')) != -1) b = true

let arr_str_a = new Array();
let arr_str_b = new Array();



if (a)
{
    if (string.indexOf('a') != -1) arr_str_a = string.split('a')
    else if (string.indexOf('а') != -1) arr_str_a = string.split('а')

    arr_str_a.shift()
    str_a = (arr_str_a[0].substr(arr_str_a[0].indexOf('{') + 1, arr_str_a[0].indexOf('}') - 2)).split(',')
}

if (b) 
{
    if (string.indexOf('b') != -1) arr_str_b = string.split('b')
    else if (string.indexOf('б') != -1) arr_str_b = string.split('б')

    arr_str_b.shift()
    str_b = (arr_str_b[0].substr(arr_str_b[0].indexOf('{') + 1, arr_str_b[0].indexOf('}') - 2)).split(',')
    
}

subString = string.split('а')
console.log(arr_str_b)//(2,0) , а={-1.944, 0.976};
alert(str_a[1] + ' dick ' + str_b)//(6,3) , а={-4.167, 7.940, -9.397, 7.515, -3.752, 0.862}; b={-2.28, 1.77, -0.472};*/
